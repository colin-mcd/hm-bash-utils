#!/usr/bin/env bash
# Source this file at the start of each other script in this dir

# cd to repo root directory
cd "$(git rev-parse --show-toplevel)"

# Clone submodules
git submodule update --init --recursive
